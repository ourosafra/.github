name: Route issue to Ouro Safra Projects

on:
  workflow_call:
    inputs:
      issue_number:
        required: true
        type: number
      repository:
        required: true
        type: string
      event_action:
        required: false
        type: string
    secrets:
      projects_app_private_key:
        required: true

permissions:
  contents: read
  issues: read

jobs:
  route:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Generate GitHub App token
        id: token
        uses: actions/create-github-app-token@d72941d797fd3113feb6b93fd0dec494b13a2547
        with:
          app-id: 4731476
          private-key: ${{ secrets.projects_app_private_key }}
          owner: ourosafra

      - name: Checkout automation repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          repository: ourosafra/.github
          ref: main
          path: automation
          token: ${{ steps.token.outputs.token }}

      - name: Route issue
        env:
          GH_TOKEN: ${{ steps.token.outputs.token }}
          ISSUE_NUMBER: ${{ inputs.issue_number }}
          REPOSITORY: ${{ inputs.repository }}
          ORGANIZATION: ourosafra
          EVENT_ACTION: ${{ inputs.event_action }}
        run: node automation/scripts/route-issues-projects.js
